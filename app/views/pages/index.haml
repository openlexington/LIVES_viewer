.row
  .span12
    %h1
      LIVES Viewer
      %small
        Simple Web App to explore LIVES data
.row
  .span6.offset3
    = form_tag('/business_search', {class: 'form-search'}) do
      = text_field_tag :query, nil, {class: 'search-query', placeholder: 'Search for a business', id: 'search'}
      = submit_tag "Search", {class: 'btn'}
